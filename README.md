# Companion
Sculptural companion lamp — voice keywords -> TouchDesigner states.

## Paths
- TD project: `TD/Companion.toe`
- Python bridge: `python/` (venv at `python/.venv`)
- Assets (LFS): `Assets/`

## OSC contract
Host `127.0.0.1`, Port `9000`
Messages: `/keyword/love 1`, `/keyword/hate 1`, `/keyword/stop 1`
